<?php

/**
 * @file
 */

/**
 * Implements hook_theme().
 */
function current_location_theme($existing, $type, $theme, $path) {
  return [
    'current_time_template' => [
      'variables' => ['time' => NULL],
    ],
  ];
}
